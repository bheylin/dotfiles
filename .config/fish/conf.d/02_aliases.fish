alias ls="eza"
alias ll="ls -l"
alias z="zoxide"

# git repo that tracks personal dotfiles
alias cfg='git --git-dir=$HOME/.cfg/ --work-tree=$HOME'
# git repo that tracks work dotfiles
alias cfg-ds='git --git-dir=$HOME/.cfg-ds/ --work-tree=$HOME'
alias gg='br --conf ~/.config/broot/git-diff-conf.toml --git-status'
