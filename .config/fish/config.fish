# the fisher plugin manager provides ssh-agent and fzf integration
#
# fisher install PatrickF1/fzf.fish
# fisher install danhper/fish-ssh-agent

# either i3 or xrvt is positioning the prompt at a const distance from
# end of the window. Clearing the terminl buffer side-steps the issue
clear
